{
  "scripts": {
    "lint": "prettier --write . && markdownlint-cli2 --fix . && eslint --fix . && stylelint --fix **/*.css"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.8.0",
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.9.6",
    "stylelint": "^17.14.1",
    "stylelint-config-standard": "^40.0.0"
  }
}
